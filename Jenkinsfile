pipeline {
  agent any
  stages {
    stage('check Docker') {
      steps {
        sh 'echo "Hello version"'
      }
    }

    stage('Print message') {
      steps {
        echo 'Docker present'
      }
    }

  }
}
